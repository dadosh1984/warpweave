interface:
  display_name: "Release Spectrix"
  short_description: "Audit, prepare, publish, and finalize releases"
  default_prompt: "Use $release-spectrix to audit the current release state and take the next safe release step."
